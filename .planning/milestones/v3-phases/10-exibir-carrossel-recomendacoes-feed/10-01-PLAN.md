---
phase: 10-exibir-carrossel-recomendacoes-feed
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/hooks/usePreferencesWizard.js
  - frontend/src/services/useRecommendations.ts
  - frontend/src/app/dashboard/index.tsx
  - frontend/src/hooks/__tests__/usePreferencesWizard.test.js
  - frontend/src/services/__tests__/useRecommendations.test.ts
autonomous: false
requirements:
  - SDGEU-22-FE
  - SDGEU-377
  - SDGEU-154

must_haves:
  truths:
    - "Ao iniciar o app nativo pela primeira vez, solicita permissões de Câmera E Geolocalização"
    - "Ao iniciar o app na Web, solicita apenas permissão de Geolocalização"
    - "Em caso de recusa/falha de geolocalização, utiliza como fallback João Pessoa, PB (-7.1196, -34.8450)"
    - "Não é possível passar de pilar no Preferences Wizard sem selecionar ao menos uma categoria daquele pilar"
    - "Carrossel do topo exibe 10 recomendações horizontais"
    - "Feed inferior exibe recomendações de 10 em 10 por padrão, mudando para feed geral de busca caso filtros manuais de categoria estejam ativos"
  artifacts:
    - path: "frontend/src/services/useRecommendations.ts"
      provides: "Hook de consulta paginada ao endpoint de recomendações"
    - path: "frontend/src/hooks/__tests__/usePreferencesWizard.test.js"
      provides: "Cobertura de testes unitários para regras de validação do wizard"
  key_links:
    - from: "frontend/src/app/dashboard/index.tsx"
      to: "/api/v1/attractions/recommendations"
      via: "Chamada do hook useRecommendations"
---

<objective>
Implementar a geolocalização ativa de inicialização com fallbacks geográficos precisos, reforçar a validação por pilar no onboarding de preferências (Preferences Wizard) e acoplar a listagem paginada inteligente no carrossel de recomendações e no feed principal do painel do explorador.

Purpose: Resolver o problema de "cold start", unificar o design visual de recomendações de forma sutil e natural, e engajar o explorador com atrações sob medida de acordo com sua geolocalização real ou o centro urbano padrão do MVP.
Output: Dependências instaladas, hooks de recomendação e wizard criados e expandidos com cobertura robusta de testes de integração e unitários.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
@~/.gemini/antigravity/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-exibir-carrossel-recomendacoes-feed/10-CONTEXT.md
@.planning/phases/10-exibir-carrossel-recomendacoes-feed/10-RESEARCH.md
@.planning/phases/10-exibir-carrossel-recomendacoes-feed/10-UI-SPEC.md
@frontend/src/app/dashboard/index.tsx
@frontend/src/hooks/usePreferencesWizard.js
@frontend/src/services/api.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalação do expo-location e fluxo de permissões ativa + fallbacks</name>
  <files>frontend/package.json, frontend/src/app/dashboard/index.tsx</files>
  <read_first>frontend/package.json, frontend/src/app/dashboard/index.tsx</read_first>
  <action>
    1. Executar no terminal do frontend: npx expo install expo-location.
    2. Modificar o arquivo `frontend/src/app/dashboard/index.tsx` para importar `* as Location from 'expo-location'` e `* as ImagePicker from 'expo-image-picker'`.
    3. Implementar lógica assíncrona no useEffect de carregamento do Dashboard:
       - Detectar a plataforma atual: se `Platform.OS === 'web'`, disparar apenas a solicitação de permissão de geolocalização via `Location.requestForegroundPermissionsAsync()`.
       - Se for ambiente nativo (iOS/Android), solicitar de forma sequencial ou paralela a permissão da câmera (`ImagePicker.requestCameraPermissionsAsync()`) E a permissão de geolocalização (`Location.requestForegroundPermissionsAsync()`).
       - Se a permissão de geolocalização for concedida, recuperar a posição atual do dispositivo usando `Location.getCurrentPositionAsync({ accuracy: Location.Accuracy.Balanced })`.
       - Se for negada, indisponível ou ocorrer falha, configurar como fallback as coordenadas da capital paraibana: latitude `-7.1196`, longitude `-34.8450`.
       - Guardar as coordenadas no estado `coords` (formato `{ latitude: number, longitude: number }`).
  </action>
  <verify>Verificar no terminal que expo-location foi instalado e o build do app não quebra por falta de importações.</verify>
  <acceptance_criteria>
    - frontend/package.json contém a linha `"expo-location"`
    - frontend/src/app/dashboard/index.tsx contém chamadas para `requestForegroundPermissionsAsync`
    - frontend/src/app/dashboard/index.tsx possui constantes para latitude `-7.1196` e longitude `-34.8450` como fallback
  </acceptance_criteria>
  <done>Permissões solicitadas e coordenadas carregadas com o fallback João Pessoa configurado</done>
</task>

<task type="auto">
  <name>Task 2: Validação estrita por pilar no usePreferencesWizard e criação de testes</name>
  <files>frontend/src/hooks/usePreferencesWizard.js, frontend/src/hooks/__tests__/usePreferencesWizard.test.js</files>
  <read_first>frontend/src/hooks/usePreferencesWizard.js, frontend/src/constants/onboarding.js</read_first>
  <action>
    1. Atualizar `frontend/src/hooks/usePreferencesWizard.js` para integrar validação antes de avançar nos passos:
       - No hook, carregar a lista de categorias do sistema através da chamada do `preferenceService.getCategories()`.
       - Mapear cada etapa com o pilar de onboarding correspondente daquele step (`gastronomia`, `cultura`, `aventura`, `relaxamento`, `noite`).
       - Ao chamar `handleNext()` ou `handleFinish()`, obter o pilar associado ao passo corrente.
       - Filtrar todas as categorias cujo `parentCategory` (ou campo que indica o pilar) seja equivalente ao pilar da etapa atual.
       - Verificar se pelo menos uma dessas categorias possui o seu identificador/slug contido dentro de `selectedIds`.
       - Se sim, permitir transição de passo (`setCurrentStep(prev => prev + 1)` ou conclusão). Se não, disparar um aviso via `Alert.alert("Exploração sob Medida", "Por favor, selecione pelo menos um interesse nesta categoria para continuarmos a montar suas recomendações.")` e retornar impedindo o avanço.
    2. Criar a suíte de testes unitários Jest em `frontend/src/hooks/__tests__/usePreferencesWizard.test.js`:
       - Mockar `preferenceService.getCategories()` retornando uma lista mock de categorias com pilares.
       - Simular transição com `selectedIds` vazio, validando que a função retorna bloqueada e o `Alert.alert` é chamado.
       - Simular transição com `selectedIds` contendo uma categoria do pilar ativo, validando que avança de passo normalmente.
  </action>
  <verify>Executar a suíte de testes unitários do wizard: npm --prefix frontend test -- usePreferencesWizard</verify>
  <acceptance_criteria>
    - usePreferencesWizard.js contém validação por pilar ativo contra selectedIds
    - usePreferencesWizard.js possui Alert.alert de impedimento amigável
    - usePreferencesWizard.test.js passa de forma limpa sem erros
  </acceptance_criteria>
  <done>Validação do Preferences Wizard concluída e coberta por testes unitários Jest</done>
</task>

<task type="auto">
  <name>Task 3: Desenvolvimento do hook useRecommendations e acoplamento no Dashboard</name>
  <files>frontend/src/services/useRecommendations.ts, frontend/src/app/dashboard/index.tsx, frontend/src/services/__tests__/useRecommendations.test.ts</files>
  <read_first>frontend/src/app/dashboard/index.tsx, frontend/src/services/useExploreData.ts</read_first>
  <action>
    1. Criar o hook `frontend/src/services/useRecommendations.ts`:
       - Chamar a rota GET `/api/v1/attractions/recommendations` passando `latitude`, `longitude`, `page` e `size` como parâmetros.
       - Manter estados de paginação (`page`, `hasMore`, `loading`, `recommendations`).
       - Tratar resposta baseando-se no envelope `StandardResponseDTO` (lendo `response.data.data.content`).
       - Retornar função para carregar mais itens (`loadMore`) e função de atualização (`refresh`).
    2. Integrar o hook no Dashboard (`frontend/src/app/dashboard/index.tsx`):
       - Alimentar o carrossel horizontal superior "Recomendado para você" consultando o hook com `size=15`, mas fatiando a renderização para exibir exatamente os **10 principais** itens horizontais usando `AttractionCard variant="compact"`.
       - Renderizar skeletons (`AttractionSkeleton variant="compact"`) enquanto os dados estiverem sendo carregados.
       - No feed vertical inferior "Descubra", acoplar a paginação por recomendações do hook:
         - Por padrão, sem filtros ativos, o feed vertical "Descubra" deve ser alimentado por `useRecommendations` de 10 em 10.
         - Se o usuário selecionar um filtro de categoria no carrossel superior de categorias, preço ou rating, o feed "Descubra" deve alternar dinamicamente para `useExploreData(filters)`, limpando os dados anteriores de recomendações e usando o endpoint de buscas gerais filtradas.
         - Se o filtro for cancelado, restaurar a exibição padrão de recomendações personalizadas.
    3. Criar testes em `frontend/src/services/__tests__/useRecommendations.test.ts` mockando chamadas axios e testando retorno de dados.
  </action>
  <verify>Executar testes de recomendações: npm --prefix frontend test -- useRecommendations</verify>
  <acceptance_criteria>
    - useRecommendations.ts criado e chamando endpoint /api/v1/attractions/recommendations com parâmetros de geolocalização e paginação
    - dashboard/index.tsx exibe carrossel com recomendações reais
    - dashboard/index.tsx alterna automaticamente entre recomendações paginadas (sem filtros) e busca filtrada (com filtros) no feed Descubra
    - useRecommendations.test.ts passa 100% verde
  </acceptance_criteria>
  <done>Hook de recomendações inteligente integrado ao carrossel e feed principal</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Fluxo de Onboarding Preferences, Geolocalização de Lançamento com fallbacks e Seções Recomendações/Descubra integradas no painel</what-built>
  <how-to-verify>
    1. Iniciar o app frontend usando 'npm run start' e rodar em emulador ou browser.
    2. Entrar na tela de onboarding (/preferences), tentar avançar sem marcar opções nas categorias e checar o bloqueio e alerta.
    3. Abrir o feed principal (Dashboard), autorizar a geolocalização e verificar as distâncias calculadas nos cards de recomendações.
    4. Negar a permissão e verificar se o fallback "A grande João Pessoa" organiza o carrossel a partir da capital paraibana.
    5. Clicar em um filtro de categoria no dashboard, certificar-se de que a listagem vertical Descubra muda instantaneamente para os filtros manuais e, ao limpar o filtro, retorna à listagem personalizada.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Antes de declarar a fase concluída:
- [ ] Rodar todos os testes do frontend garantindo que estão verdes: `npm --prefix frontend test`
- [ ] Checar no terminal que não existem erros de compilação ou lints pendentes no Expo.
</verification>

<success_criteria>
- Todas as tarefas técnicas e de teste implementadas e verificadas
- Bloqueio estrito de avanço de categorias sem interesses no Wizard funcional
- Recomendações por geolocalização e com fallback de João Pessoa renderizadas com performance premium
- Sincronia perfeita entre feed por afinidades (padrão) e feed por busca (com filtros ativos)
</success_criteria>

<output>
Após a conclusão, criar o relatório final `.planning/phases/10-exibir-carrossel-recomendacoes-feed/10-01-SUMMARY.md`
</output>
